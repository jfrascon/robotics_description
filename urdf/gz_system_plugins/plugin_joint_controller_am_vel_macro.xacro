<?xml version="1.0"?>
<!--
This file defines the public `plugin_joint_controller_am_vel` wrapper.
The goal of this wrapper is to emit the XML for a Gazebo Sim
`JointController` plugin for one specific integration pattern: commands arrive
in `actuator_msgs/msg/Actuators`, the plugin reads the commanded velocity from
`velocity[actuator_number]`, and the controller runs in velocity-command mode.
The incoming command is a target velocity, and Gazebo applies that target
velocity directly without using the PID fields.

This wrapper is a thin specialization of the generic
`plugin_joint_controller` macro. It decides only the transport type and the
control mode:
- `use_actuator_msg` is fixed to `true`, so this wrapper publishes the
  `<use_actuator_msg>true</use_actuator_msg>` field and also publishes
  `<actuator_number>`.
- `use_force_commands` is fixed to `false`, so this wrapper enables the
  velocity-command path in the generic macro and does not use PID fields.

The caller still has to decide:
- Which joint this plugin controls through `joint_name`.
- Which actuator entry the plugin reads through `actuator_number`.
- The initial velocity that will be published through `initial_velocity`.
- The command topic that will be published through `topic`.
-->
<robot name="plugin_joint_controller_am_vel_macro"
       xmlns:xacro="http://www.ros.org/wiki/xacro"
       xmlns:gz="http://gazebosim.org/schema">
  <xacro:include filename="$(find robotics_description)/urdf/gz_system_plugins/plugin_joint_controller_macro.xacro"/>

  <xacro:macro name="plugin_joint_controller_am_vel"
               params="joint_name
                       actuator_number
                       initial_velocity:='0.0'
                       topic">
    <!--
    This wrapper forwards the caller-provided plugin fields unchanged and only
    fixes the transport type and control mode to the actuator-message +
    velocity-command combination described above.
    -->
    <xacro:plugin_joint_controller
      joint_name="${joint_name}"
      use_actuator_msg="True"
      actuator_number="${actuator_number}"
      use_force_commands="False"
      initial_velocity="${initial_velocity}"
      topic="${topic}"/>
  </xacro:macro>
</robot>
